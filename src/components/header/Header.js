import '.header.scss';
import {NavBar, Nav, Container} from 'react-bootstrap';

const Header = () => {
    
    return(
        <NavBar>
            <Container>
                
            </Container>
        </NavBar>
    )
}

export default Header;